[tasks.install]
run = "pnpm install --filter @kondis/web --frozen-lockfile"

[tasks.dev]
env._.path = "./node_modules/.bin"
run = "vite dev"

[tasks.build]
env._.path = "./node_modules/.bin"
run = "vite build"

[tasks.check]
env._.path = "./node_modules/.bin"
run = "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
